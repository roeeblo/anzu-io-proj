services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    # Optional: use MongoDB in another container
    # env_file: .env
    # depends_on:
    #   - mongo

  # Uncomment to run MongoDB in Docker (then set MONGODB_URI=mongodb://mongo:27017 for server)
  # mongo:
  #   image: mongo:7
  #   ports:
  #     - "27017:27017"
